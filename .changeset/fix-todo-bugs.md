---
'@brainjar/cli': patch
---

Fix project-scoped overrides (envelope unwrap, sync scope passthrough, workspace query isolation), race condition in ensureRunning with file lock, ETXTBSY on upgrade by stopping server before binary replacement, bump MIN_SERVER_VERSION to 0.2.4.
